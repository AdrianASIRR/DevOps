stages:
  - setup

setup_job:
 stage: setup
 script:
   - echo "Iniciando fase de setup..."
   - echo "Preparando entorno de CI..."
   # Aquí podrías añadir instalación de dependencias, por ejemplo:
   # apt-get update && apt-get install -y curl
   # - npm install
   - echo "Setup completado correctamente."
